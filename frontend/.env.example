# T32 - URL du backend consommee par frontend/src/api/client.ts.
# Vite ne lit les variables VITE_* que depuis un .env place ICI
# (frontend/), pas depuis la racine du depot.
VITE_API_BASE_URL=http://localhost:8000
